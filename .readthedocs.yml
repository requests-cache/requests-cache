# Read the Docs configuration file
version: 2

sphinx:
  builder: html
  configuration: docs/conf.py

build:
  image: 8.0

python:
  version: 3.8
  system_packages: True
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
        - backends
